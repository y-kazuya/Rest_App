
- if @owner.errors.any?
  #error_explanation
    .alert.alert-danger
      = "The form contains #{pluralize(@owner.errors.count, "error")}."
    %ul
      - @owner.errors.full_messages.each do |msg|
        %li= msg
